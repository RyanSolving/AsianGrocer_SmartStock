{
  "name": "asiangrocer-smartstock-workspace",
  "private": true,
  "dependencies": {
    "next": "14.2.3"
  },
  "scripts": {
    "prebuild": "npm --prefix web-app install",
    "dev": "npm --prefix web-app run dev",
    "build": "npm --prefix web-app run build",
    "start": "npm --prefix web-app run start",
    "lint": "npm --prefix web-app run lint"
  }
}
